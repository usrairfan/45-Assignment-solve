import { Interface } from "readline";

    // Print Person
interface person{
name:string,
age:Number,
nationality:string,
married:boolean,
}
let object:person ={
name :         'usra',
age:           35,
nationality: 'Pakistani',
married:       true
}
console.log(object);
       //Print Car
    interface car {
         maker:String,
         colour:String,
         automatic:Boolean

       }
const car={
    maker:'Toyota',
    colour:'White',
    automatic:true
}
console.log(car);

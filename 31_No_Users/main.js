var user = ["Admin", "Saima", "Tahira", "Laiba", "Sana"];
if (user.length === 0) {
    console.log("We need to some users!");
}
else {
    user = [];
    console.log("All user name have been removed." + user.length);
}
